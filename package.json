{
  "name": "rioterm-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "build:wasm": "node scripts/build-wasm.mjs",
    "build": "npm run build -w rioterm && npm run build -w react-rioterm",
    "test": "npm test -w rioterm",
    "dev": "npm run dev -w docs",
    "build:docs": "npm run build -w docs",
    "deploy:cf": "PAGES_BASE=/ npm run build -w docs && npx wrangler pages deploy docs/dist --project-name riotermjs"
  }
}
